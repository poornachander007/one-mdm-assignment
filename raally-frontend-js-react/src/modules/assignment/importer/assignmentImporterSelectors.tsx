import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const assignmentImporterSelectors = importerSelectors(
  'assignment.importer',
);

export default assignmentImporterSelectors;
