import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const compensationImporterSelectors = importerSelectors(
  'compensation.importer',
);

export default compensationImporterSelectors;
